import { ActivityHistoryPage } from "@/modules/packages/activity";

/** /activity-history — documentation page explaining each activity type. */
export default function ActivityHistory() {
  return <ActivityHistoryPage />;
}